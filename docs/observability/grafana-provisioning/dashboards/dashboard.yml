apiVersion: 1

providers:
  - name: rbac-ums
    folder: rbac-ums
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
